spring.application.name=thirty-api

# URL de Supabase (La que te pasé antes)
spring.datasource.url=jdbc:postgresql://db.kedomeaedwtyzuzrboky.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Bonfireeasyjet47!

# Configuración de Driver
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Puerto dinámico para Render (OBLIGATORIO)
server.port=${PORT:8080}

# Multipart
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
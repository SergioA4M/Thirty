spring.application.name=thirty-api

# URL usando el puerto 6543 (Pooler) y par\u00E1metros de estabilidad
spring.datasource.url=jdbc:postgresql://db.ylxpcytucebvzijqyrsx.supabase.co:6543/postgres?prepareThreshold=0&ssl=true&sslmode=require
spring.datasource.username=postgres
spring.datasource.password=Easyjet47Sergio
spring.datasource.driver-class-name=org.postgresql.Driver

# Forzamos dialecto y creaci\u00F3n de tablas
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Ajuste de Hikari para evitar cortes
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

server.port=${PORT:8080}